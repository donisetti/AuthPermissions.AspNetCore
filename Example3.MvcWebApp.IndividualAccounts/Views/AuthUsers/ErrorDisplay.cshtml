@model string

@{
    ViewData["Title"] = "Tela de erro";
}

@foreach (var error in @Model.Split(Environment.NewLine))
{
    <h3 class="text-danger">@error</h3>
}

<div>
    <a asp-action="Index">Voltar ao Inicio</a>
</div>
